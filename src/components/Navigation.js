const Nav = () => {
    const template = `
      <header class="header">
        <h1 class="h_title">My parcel app</h1>
        <p class="h_text">This is parcel startup boilerplate that supports creating components and reuse them, along with babel ,postcss, autoprefixer ...</p>
      </header>
    `;

    return template;
}

export default Nav;